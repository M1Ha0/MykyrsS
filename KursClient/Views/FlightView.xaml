<UserControl x:Class="KursClient.Views.FlightView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KursClient.Views"
             xmlns:vm="clr-namespace:KursClient.ViewModels"
             xmlns:Menu="clr-namespace:KursClient.Utills"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Style="{StaticResource Page_Style}">
    <UserControl.DataContext>
        <vm:FlightViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Border CornerRadius="0,20,20,0" Margin="0,5,5,0">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="9*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource ToolBarButtonBaseStyle}"
                            Command="{Binding AddCommand}" Width="30"
                            Height="30" HorizontalAlignment="Right">
                        <Image Source="/Assets/add_one_button_insert_plus_icon.png"/>
                    </Button>
                    <Button Style="{StaticResource ToolBarButtonBaseStyle}"
                         Command="{Binding DeleteCommand}" CommandParameter="{Binding Selected}"
                        Width="30"
                        Height="30" HorizontalAlignment="Right">
                        <Image Source="/Assets/delete_icon.png"/>
                    </Button>
                </StackPanel>
                <DataGrid ItemsSource="{Binding FlightList}"
                          AutoGenerateColumns="False"
                          Grid.Row="1" IsReadOnly="True" 
                          SelectedItem="{Binding Selected}" FontSize="14">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <i:InvokeCommandAction
                                Command="{Binding EditCommand}"
                                CommandParameter="{Binding Selected}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Изменить" 
                                      Command="{Binding EditCommand}"
                                      CommandParameter="{Binding Selected}"/>
                            <MenuItem Header="Удалить" 
                                      Command="{Binding DeleteCommand}"
                                      CommandParameter="{Binding Selected}"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Номер Рейса" Width="Auto" Binding="{Binding NumberFlight}"/>
                        <DataGridTextColumn Header="Самолет" Width="Auto" Binding="{Binding Аirplane}"/>
                        <DataGridTextColumn Header="Откуда" Width="Auto" Binding="{Binding Departure}"/>
                        <DataGridTextColumn Header="Куда" Width="Auto" Binding="{Binding Destination}"/>
                        <DataGridTextColumn Header="Дата вылета" Width="Auto" Binding="{Binding Date}"/>
                        <DataGridTextColumn Header="Время вылета" Width="Auto" Binding="{Binding TimeDeparture}"/>
                        <DataGridTextColumn Header="Время полета" Width="Auto" Binding="{Binding TimeFlight}"/>
                        <DataGridTextColumn Header="Цена" Width="Auto" Binding="{Binding Price}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
